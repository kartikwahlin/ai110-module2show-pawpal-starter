classDiagram
    class Task {
        +str name
        +int duration
        +int priority
    }

    class Pet {
        +str name
        +list~Task~ tasks
    }

    class Owner {
        +str name
        +int day_start
        +int day_end
        +list~Pet~ pets
        +add_pet(pet: Pet)
        +remove_pet(pet: Pet)
        +get_all_tasks() list~Task~
    }

    class Scheduler {
        +Owner owner
        +get_tasks() list~Task~
        +organize_tasks() list~Task~
        +generate_times() list~tuple~
    }

    Pet "1" --> "0..*" Task : has
    Owner "1" --> "1..*" Pet : manages
    Scheduler "1" --> "1" Owner : uses
